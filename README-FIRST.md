# NervOS Repository Bootstrap Note

The architecture documentation and Claude Code project governance bundle have been copied into this repository.

`docs/implementation-status.md` is the source of truth for what is actually implemented. Other documents may describe later target architecture and must not be read as current product behavior.

Use `README.md` for current development instructions. Keep shared project instructions, rules, hooks, skills, and reviewer definitions versioned; keep machine-local settings, worktrees, plans, logs, caches, transcripts, credentials, and other local state untracked.
